MDN Web Docs - Front 초보자용 

JavaScript에서 var a = 10; var a = 20; 두 번 선언가능
선언 못하게 하려면 let a = 10; 처럼 let 혹은 const 로 선언해야함

var는 변수명이 여러번 선언, 마지막 선언시 대입한 값( 자료형포함 )을 따름.
let은 지역변수 (변수명이 유니크한 값을 '나' 만 쓰고싶을때)
const는 immutable(불변) 


window 객체의 자식객체 - screen location history document

element - 열리는 태그와 닫히는 태그

js에서 [ ] -> array, { } ->object

호출시 이름만 적어줌 -> ( ) X -> document.getElementById("btn-2").onclick = dtn2clickHandler;

js언어의 특징
1.이벤트 발생시 콜백함수를 등록 (호출은 자바스크립트가 해줌) -> 등록시  ( ) 없음.  ex) export defaut App;
2. 등록한 함수 안에서 가장 많이 하는 행동 -> ajax ( DB에서 정보를 읽어와 화면에 Display를 다시 해주기 위해 )


--- REACT ---
궁극적으로 div태그로 

nodejs.org -> 18.17.1 LTS 다운로드 -> 설치 (programfiles\node~~~) -> auto x -> 설치 완료후 VS Code 실행